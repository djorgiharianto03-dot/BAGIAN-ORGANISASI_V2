# Tambahkan di blok server {} CloudPanel (Vhost → Advanced) jika perlu.
# Mencegah eksekusi PHP di folder uploads.

location ~ ^/uploads/.*\.php$ {
    deny all;
    return 404;
}

location ^~ /uploads/ {
    location ~ \.(php|phtml|php\d)$ {
        deny all;
        return 404;
    }
}
